# 26.2.0 version
java_version=25
minecraft_version=26.2
compatible_minecraft_versions=["26.2.0"]
accessWidenerVersion=26_1
builds_for=fabric,neoforge
# forge is broken due to gradle/build script issues

# Netty
netty_version=4.1.97.Final

# LWJGL
lwjgl_version=3.4.1

# Fabric loader
fabric_loader_version=0.19.3
fabric_api_version=0.149.0+26.2
    modmenu_version=20.0.1
    starlight_version_fabric=
    phosphor_version_fabric=
    lithium_version=
    sodium_version=
    iris_version=1.11.4+26.2-fabric
    bclib_version=
    immersive_portals_version=
    canvas_version=
    
    # vertigo - prevents DH from seeing the full chunk, causing holes
	# Iris - Iris added a new depth feature we need to track in 1.11.4, so we no longer support 1.11.3 and older
    fabric_incompatibility_list={ "vertigo": "*", "iris": "<1.11.4" }
    fabric_recommend_list={}

    # Fabric mod run
    # 0 = Don't enable and don't run
    # 1 = Can be referenced in code but doesn't run
    # 2 = Can be referenced in code and runs in client
    enable_mod_menu=1
    enable_starlight=0
    enable_phosphor=0
    enable_sodium=0
    enable_lithium=0
    enable_iris=1
    enable_bclib=0
    enable_immersive_portals=0
    enable_canvas=0

# NeoForge loader
forge_version=
neoforge_version=1-beta
neoforge_version_range=[*,)

    # NeoForge mod versions
    neo_iris_version=1.11.4+26.2-neoforge
    
    # (Neo)Forge mod run
    # 0 = Don't enable and don't run
    # 1 = Can be referenced in code but doesn't run
    # 2 = Can be referenced in code and runs in client
    neo_enable_iris=1
